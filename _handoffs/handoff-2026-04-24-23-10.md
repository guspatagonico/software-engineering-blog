## Completed

- [x] Implemented Mermaid interaction refinements in `public/vendor/mermaid-render.js`.
- [x] Updated Mermaid viewport/canvas sizing logic to reduce mobile truncation issues and keep initial canvas scale at `1.0`.
- [x] Adjusted Mermaid controls positioning and spacing near the bottom-right edge of the diagram container.
- [x] Removed container padding that was offsetting controls (`.mermaid { padding: 0; }`).
- [x] Added interaction-driven controls visibility behavior with idle auto-hide (desktop hover and mobile touch/pointer activity).
- [x] Added and refined fade-in/fade-out transitions for Mermaid controls.

## Decisions Made

- Mermaid controls should only be visible during direct interaction and auto-hide after inactivity.
- Auto-hide timeout for controls is `3000ms`.
- Initial Mermaid canvas transform scale should be `1.0`.
- Controls offset target is `8px` from right and bottom edges.
- Container-level padding in `.mermaid` must be `0` to avoid visual offset and spacing issues.
- Keep directional pan buttons removed; only `+`, `-`, and `RST` remain.

## Files Modified

- `public/vendor/mermaid-render.js`
- `src/styles/blog-post.css`
- `src/styles/post-content.css`

## Pending

- [ ] Validate on an actual mobile viewport/device that "Loop de control" is no longer truncated.
- [ ] Confirm controls fade-out is visually smooth in both desktop and mobile after inactivity.

## Blockers

- None.
