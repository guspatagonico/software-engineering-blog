## Completed

- [x] Updated homepage `.post-card` styles so light mode keeps readable card visuals when Matrix background is active.
- [x] Refactored light-mode card styling to reuse CSS custom properties instead of duplicating values.
- [x] Added a subtle frosted-glass effect to light-mode `.post-card` via backdrop blur/saturation.
- [x] Increased overall `.post-card` translucency while preserving readability in dark mode and Matrix mode.
- [x] Ran verification after style changes (`rtk lint` and `pnpm typecheck`) with no errors.

## Decisions Made

- Light mode + Matrix should prioritize readability and visual consistency with normal light-mode cards.
- Glassmorphism is applied subtly (not heavy blur) using shared CSS variables for maintainability.
- Translucency was increased incrementally across regular dark cards, hover states, and Matrix dark states rather than applying a single aggressive change.

## Files Modified

- `src/styles/post-list.css`

## Pending

- [ ] Do a visual pass in browser to fine-tune translucency/blur balance if needed across light, dark, and Matrix states.

## Blockers

- None.
