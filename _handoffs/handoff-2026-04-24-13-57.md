## Completed

- [x] Updated section selector item color to a lighter near-white tone for dark mode in `src/components/SectionNav/SectionNav.css`.
- [x] Added light-theme override for section selector item color to preserve readability in `src/components/SectionNav/SectionNav.css`.
- [x] Implemented a first attempt to sync `.section-nav-toggle__icon` fade animation with the mobile next-hint arrows by adding conditional class wiring in `src/components/SectionNav/SectionNav.tsx` and keyframes/class styles in `src/components/SectionNav/SectionNav.css`.
- [x] Captured user feedback that the sync attempt did not work visually (icon still not fading in/out in sync with `.section-nav-next-hint` arrows).

## Decisions Made

- Apply the section text color change directly in the shared `SectionNav` component styles so it affects blog posts consistently.
- Gate toggle-icon hint animation behind the same runtime conditions used to show the mobile next-hint (`showNextHint`, `hasNextSection`, and collapsed mobile state).
- Keep the current attempt in code for iteration instead of reverting immediately, pending a follow-up fix based on observed behavior.

## Files Modified

- `src/components/SectionNav/SectionNav.css`
- `src/components/SectionNav/SectionNav.tsx`

## Pending

- [ ] Fix the mobile collapsed-state animation so `.section-nav-toggle__icon` visibly fades in/out at the same time as `.section-nav-next-hint` arrow animation.
- [ ] Verify behavior on an actual mobile viewport/state transition (collapsed nav, near-end scroll, next section available).

## Blockers

- No hard blocker, but current animation trigger/visibility interplay is not producing the expected synced fade effect in the rendered UI.
