## Completed

- [x] Implemented centered content UX with a new `.content-viewport` wrapper around `.content` in all current blog posts.
- [x] Moved desktop middle-column scrolling to `.content-viewport` while keeping `.content` width-constrained and centered.
- [x] Kept right metadata rail aligned to the far right on desktop layout.
- [x] Updated scroll-dependent behavior to prioritize `.content-viewport` (header compact metrics/listeners, hash section reset scroll, SectionNav scroll target, ScrollIndicator target).
- [x] Produced a breakpoint/features table for Section Selector behavior across global/mobile/tablet/desktop.
- [x] Made tablet (`768-1023`) Section Selector behavior match mobile with minimal diff (collapse defaults, toggle UX, next hint eligibility, auto-collapse on section select).
- [x] Ran verification commands: `pnpm typecheck` (pass) and `pnpm lint` (fails on pre-existing unrelated issues).

## Decisions Made

- Use a two-layer middle column (`.content-viewport` + `.content`) to decouple scroll container behavior from centered content width.
- Keep desktop behavior unchanged except for middle scroll container and right-rail alignment refinements.
- Preserve existing fallbacks (`.content` -> `.post-layout` -> `window`) in JS scroll target resolution for robustness.
- Treat tablet as mobile-like for Section Selector by using `max-width: 1023px` in both JS media queries and CSS responsive block.
- Minimize risk by removing duplicated tablet CSS rules and extending the mobile rule block instead.

## Files Modified

- `src/components/SectionNav/SectionNav.css`
- `src/components/SectionNav/SectionNav.tsx`
- `src/content/blog/bienvenidos.mdx`
- `src/content/blog/harness-agentic-control.mdx`
- `src/content/blog/harness-engineering.mdx`
- `src/content/blog/llm-context-limitations.mdx`
- `src/layouts/BlogPost.astro`
- `src/styles/blog-post.css`
- `src/styles/post-content.css`

## Pending

- [ ] Perform visual QA on breakpoints `768`, `834`, and `1023` to confirm full mobile-parity behavior for Section Selector on tablet.
- [ ] Optionally tune middle-column spacing/gutters after UX review.

## Blockers

- `pnpm lint` is currently blocked by pre-existing unrelated lint errors in files not modified during this session (`src/components/D2DiagramRenderer.tsx`, `src/lib/rehypeD2.mjs`, `test-dark.mjs`, `test-overrides.mjs`, `test-themes.mjs`).
