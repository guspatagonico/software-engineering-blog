## Completed

- [x] Diagnosed the scroll capture issue on static pages (`/about`, `/now`, `/contact`) where wheel/touch scrolling only worked while hovering `.content`.
- [x] Updated `src/layouts/Base.astro` to use the blogpost-style wrapper structure: `.page-container > .content-viewport > .content`.
- [x] Moved vertical scroll ownership from `.content` to `.content-viewport` and kept `.content` as the centered content column.
- [x] Updated `ScrollIndicator` target in `src/layouts/Base.astro` from `.content` to `.content-viewport`.
- [x] Updated `src/components/Dodecahedron/Dodecahedron.tsx` to prioritize `.content-viewport` as the scroll listener target, with fallbacks.
- [x] Ran `pnpm typecheck` successfully (0 errors).

## Decisions Made

- Adopt the same scroll-container pattern used in blog posts for static pages to make scroll capture full-width while preserving centered content.
- Keep `.content` constrained (`max-width`) for readability, and let `.content-viewport` handle scrolling behavior.
- Keep robust scroll target fallback chain in dodecahedron auto-hide logic: `.content-viewport` -> `.content` -> `.page-container`.
- Limit scope to layout/scroll behavior only; no unrelated refactors.

## Files Modified

- `src/layouts/Base.astro`
- `src/components/Dodecahedron/Dodecahedron.tsx`

## Pending

- [ ] Manually verify wheel and touch scrolling over lateral margins on:
- [ ] `http://localhost:4321/software-engineering/about`
- [ ] `http://localhost:4321/software-engineering/now`
- [ ] `http://localhost:4321/software-engineering/contact`

## Blockers

- None.
