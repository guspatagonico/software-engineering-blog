# Handoff — 2026-04-02 01:42

## Completed

- [x] New blog post: "Limitaciones de los LLM en la actualidad: Ventana de contexto y Smart Context"
  - Standalone Astro page at `src/pages/blog/llm-context-limitations.astro`
  - Covers context window limitations, Smart Context (÷2), strategies: MCPs, skills, compaction, handoffs, RTK
  - Interactive `ContextWindowChart` component (React island) with toggle between full context and Smart Context
  - Chart compares 9 models: Gemini 1.5 Pro/2.5 Pro, GPT-4 Turbo/4o/4.1, Claude 3 Opus/3.5 Sonnet/Opus 4/Sonnet 4
  - Added to blog index with tags `llm`, `context`
- [x] Content config migrated from legacy `type: 'content'` to `glob` loader (`astro/loaders`)
  - Fixes "The collection blog does not exist or is empty" error in Astro 6
  - `post.slug` → `post.id` in `blog/index.astro` and `pages/index.astro`
- [x] ThemeToggle hydration mismatch fixed
  - `client:load` → `client:only="react"` in Navbar.astro (skips SSR entirely)
  - Store reads `data-theme` attribute from `<html>` before `localStorage` (aligns with inline script)
  - Icons inverted: ☾ dark mode, ☀ light mode
  - Fixed-width button (`width: 32px`, `text-align: center`) prevents left-right shift

## Decisions Made

- Blog post as Astro page (not MDX collection) to support interactive React island
- `ContextWindowChart` uses pure CSS bar chart (no external library)
- `client:only="react"` for ThemeToggle — simplest fix for SSR/client state mismatch
- `z` deprecation warnings in content.config.ts left as-is (Astro 6 / zod v4)

## Files Created

- `src/components/ContextWindowChart/ContextWindowChart.tsx`
- `src/components/ContextWindowChart/ContextWindowChart.css`
- `src/pages/blog/llm-context-limitations.astro`

## Files Modified

- `src/content.config.ts` — glob loader
- `src/pages/blog/index.astro` — new post entry, `post.slug` → `post.id`
- `src/pages/index.astro` — `post.slug` → `post.id`
- `src/components/Navbar.astro` — `client:only="react"`
- `src/components/ThemeToggle/ThemeToggle.tsx` — inverted icons
- `src/components/ThemeToggle/ThemeToggle.css` — fixed width
- `src/stores/themeStore.ts` — read `data-theme` attr first

## Pending

- [ ] Vitest setup (`pnpm test`)
- [ ] Additional blog posts

## Blockers

none

## Result

DONE — new LLM context limitations post with interactive chart, Astro 6 content config migration, ThemeToggle hydration fix.
