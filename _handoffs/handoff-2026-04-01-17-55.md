# Handoff — 2026-04-01 17:55

## Completed

- [x] Font size improvements for readability:
  - `.post-subtitle`: 10px → 12px (BlogPost.astro, index.astro)
  - `.subtitle`: 10px → 12px (about, now, contact, blog/index, harness-engineering)
  - `h3`: 10px → 12px (global.css)
  - `.card-title`: 10px → 12px (Card.astro)
  - `.navbar-link`: 11px → 13px (Navbar.astro)
  - `.post-list .post-title`: 14px → 16px (index.astro)
  - `.link-label`: 10px → 12px (contact.astro)
  - `.section-nav__label`: 9px → 11px
  - `.section-nav__button`: 13px → 15px
  - `.section-nav__icon`: 12px → 14px
- [x] Inline styles extracted from harness-engineering.astro:
  - 5× `style="color:var(--teal)"` on `<strong>` → `.teal` CSS class
- [x] React components converted from inline styles to CSS modules:
  - `SectionNav.tsx` → `SectionNav.css` (BEM: `section-nav__*`)
  - `ProjectTabs.tsx` → `ProjectTabs.css` (BEM: `project-tabs__*`)
  - `Checklist.tsx` → `Checklist.css` (BEM: `checklist__*`)
  - `ThemeToggle.tsx` → `ThemeToggle.css` (BEM: `theme-toggle`)
- [x] CSS modules replaced with regular CSS to avoid hashed class names in output HTML
- [x] Deleted all `.module.css` files

## Decisions Made

- CSS modules converted to regular CSS — React islands are isolated, no scoping risk
- BEM naming convention (block\_\_element--modifier) for all component CSS classes
- One remaining dynamic `style` kept: `checklist__progress-fill` width percentage (runtime computed)
- `ConvergentEnvelope.tsx` left with inline SVG attributes (SVG presentation attributes, not CSS)

## Commits

1. `feat: scaffold project with documentation, tooling, and seed template`
2. `feat: add harness engineering post with Astro + React islands`
3. `feat: add infrastructure, pages, and navigation`
4. `fix: improve readability with larger font sizes`

## Pending

- [ ] Vitest setup (`pnpm test`)
- [ ] Additional blog posts (content collections are ready, just need MDX files)
- [ ] Dark/light token values reviewed for accessibility contrast

## Blockers

none

## Result

DONE — font sizes normalized, all inline styles extracted, CSS class names cleaned up with BEM convention.
