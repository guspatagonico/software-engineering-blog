## Completed

- [x] Reverted D2 diagram text back to default D2 font by removing the Inter override in `src/styles/d2-diagram.css`.
- [x] Removed all custom touch handlers for D2 diagrams to restore native mobile/tablet browser scrolling behavior in `src/components/D2DiagramRenderer.tsx`.
- [x] Switched diagram viewport touch behavior to native by setting `touch-action: auto` in `src/styles/d2-diagram.css`.
- [x] Hid diagram navigation controls on touchscreens (`@media (hover: none) and (pointer: coarse)`) in `src/styles/d2-diagram.css`.
- [x] Fixed oversized diagram containers on mobile/touch by overriding min-height constraints at the end of `src/styles/d2-diagram.css` so container height matches actual diagram content height.

## Decisions Made

- Use native browser gestures/scroll as the only interaction model on touch devices (no custom touch pan for diagrams).
- Keep desktop mouse-based pan controls intact while disabling/hiding touch-specific controls.
- Apply touchscreen targeting using `(hover: none) and (pointer: coarse)` to affect phones/tablets without impacting desktop pointer behavior.
- Resolve mobile container whitespace by enforcing touch-device min-height overrides after existing mobile breakpoints to win CSS cascade conflicts.

## Files Modified

- `src/components/D2DiagramRenderer.tsx`
- `src/styles/d2-diagram.css`

## Pending

- [ ] Validate final mobile/tablet behavior on real devices (or emulator) for all 3 diagrams in both light and dark themes.

## Blockers

- None.
